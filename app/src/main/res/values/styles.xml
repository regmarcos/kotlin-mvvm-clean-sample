<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MarvelTitleTextView">
        <item name="android:textSize">@dimen/text_size_card_view</item>
        <item name="android:layout_margin">@dimen/margin_card_view</item>
        <item name="android:layout_gravity">bottom|end</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAlignment">textEnd</item>
        <item name="android:elevation">@dimen/elevation_text_view_card_view</item>
        <item name="android:background">@drawable/shadow</item>
    </style>

    <style name="MarvelFloatingActionButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/fab_margin</item>
        <item name="android:backgroundTint">@color/fab_background_color</item>
    </style>

</resources>
